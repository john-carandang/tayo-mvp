TAYO — HERO IMAGE REDESIGN PROMPT
Replace the current hero section entirely. Build it exactly as described below. Do not use any other image or URL — use only the file already uploaded to /public/hero-bg.png.

STRUCTURE:
The page has two distinct zones stacked vertically:
Zone 1 — Beige nav bar (full width, sits above everything)

Full-width solid beige bar, background: #F5F0E8, height ~60px
"Tayo" logo far left — color: #C4622D (burnt orange), serif font, ~20px
"How it works" and "FAQ" centered — warm brown, 14px
"Sign up / Log in" button far right — burnt orange pill, white text
This bar sits completely above the hero image — it is not overlaid on the photo

Zone 2 — Hero (full width, image + overlaid text block)
The hero is a full-width image container, ~85vh tall:
css.hero {
  width: 100%;
  height: 85vh;
  position: relative;
  overflow: hidden;
}

.hero img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center top;
  display: block;
}
The image source is /hero-bg.png — the warm retro living room with four BIPOC young adults seated around a coffee table, warm amber lighting, vintage posters, green velvet sofa, bookshelves. Use this exact file. Do not substitute any other image or URL.
Text overlay block — centered on the image:
On top of the hero image, centered both horizontally and vertically, place a text block with a semi-transparent beige backing:
css.hero-text-block {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background: rgba(245, 240, 232, 0.85);
  padding: 32px 48px;
  border-radius: 4px;
  text-align: center;
  max-width: 640px;
  width: 90%;
}
Inside the text block:

Headline: "Drown out the noise." on line 1, "Find your signal." on line 2
Font: serif, font-size: clamp(36px, 5vw, 60px), font-weight: 700, color: #1C1008, line-height: 1.15
Subheading below: "Tayo is an AI coaching platform that helps you cut through the noise of modern life and tune into who you truly are and what truly matters most."
Subheading: font-size: 15px, color: #3D2B1F, margin-top: 12px, line-height: 1.7, max-width 480px, centered
CTA button below subheading: "Begin your journey →", burnt orange pill background: #C4622D, white text, border-radius: 24px, padding: 12px 28px, margin-top: 20px
No disclaimer text. No "free to get started." No "no credit card required."


WHAT NOT TO DO:

Do not apply a dark overlay or filter on the image — the photo should show through with full warmth
Do not use the image as a CSS background-image — use an <img> tag with object-fit: cover
Do not apply sepia, brightness, or saturate filters — the image is already warm
Do not put the nav bar inside the hero — it sits above it in normal document flow
Do not change anything below the hero (testimonials, problem/solution sections, How Tayo works)


DEFINITION OF DONE:

Beige nav bar sits above the hero — Tayo logo in burnt orange, nav links centered, Sign up button right
Hero displays /hero-bg.png full-width using an <img> tag with object-fit: cover
Semi-transparent beige text block centered on the image with correct headline, subheading, and CTA
Headline reads "Drown out the noise. Find your signal." in large dark serif
No disclaimer, no "free to get started", no dark photo overlay
Everything below the hero is unchanged

After completing, take a screenshot of the full hero and confirm the exact file path of the image being used.